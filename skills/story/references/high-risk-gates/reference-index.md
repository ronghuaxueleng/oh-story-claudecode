# 高风险第二闸门共享索引

这份索引承接 `story-short-write / story-deslop` 共用的第二闸门 prompt 资产。

这些文件和 `short-high-risk/` 不同：

- `short-high-risk/` 放的是高敏判断、自检、同桥不过检、作者站位等判断正文
- 这里放的是脚本会直接读取的 gate prompt 主文档

---

## 1. 共用 gate 主文档

- [通用-受限重写防错协议.md](通用-受限重写防错协议.md)
- [执行模板-受限重写提示词.md](执行模板-受限重写提示词.md)
- [执行模板-失败即重写判定.md](执行模板-失败即重写判定.md)

---

## 2. 维护口径

- 这 3 份文件是共享主文档。
- `story-short-write` 和 `story-deslop` 的脚本应优先读取这里。
- 两边 skill 内本地副本暂时保留，只作为运行兜底，不再视为主维护点。
