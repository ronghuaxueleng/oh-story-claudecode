# 写作执行铁律

## 目的
- 统一本项目的写作硬闸，避免不同会话、不同章节、不同批次各说各话。
- 本文件只应写入明确书名目录；未创建书籍目录时不得预生成到项目根目录。

## 读前顺序
1. 先读 `写作执行铁律.md`
2. 再读当前书目的 `追踪/上下文.md`
3. 再读当前章的 `短执行核`、`写前闸门`、`细纲`
4. 若是续写 / 审查 / 回炉，还要先核对上一章或上一章组的 `写后验收`
5. 若本章使用参考书原文、TXT 情节库或要做“参考驱动版对比”，再补读：
   - `story-setup/references/agent-references/reference-boundary-and-sources-split.md`
   - `story-setup/references/agent-references/chapter-prewrite-card-enforcement.md`
   - `story-setup/references/agent-references/reference-chapter-comparison-protocol.md`

## 硬闸
- 未写写前闸门，不得写正文。
- 未生成 `短执行核`，或 `短执行核` 仍是泛化方法提醒，不得写正文。
- 涉及参考书原文 / TXT 情节库写作时，未先明确“本章事件责任、人物责任、章尾责任”，不得写正文。
- 涉及参考书原文 / TXT 情节库写作时，禁止把参考原文章节顺序直接平移成本章正文。
- 涉及参考书原文 / TXT 情节库写作时，禁止把 `掌口 / 改口 / 执行口 / 解释口 / 资源口 / 口令 / 口子 / 第一刀 / 第二刀 / 补刀 / 追刀 / 落刀 / 解释权 / 执行权 / 决定权 / 资源位 / 头位 / 控盘 / 抢势 / 盘面` 直接写成章节任务词、收益词、爽点词；必须改写成“谁先动、谁先补、谁先分、谁先放、谁被迫认账、谁失去先手、谁先吃亏、谁先退、谁先交东西、谁先让地方”。
- `章组收益卡`、`动作骨架卡`、`人物冲突卡`、`章尾现场句预案` 缺任一项时，不得把“已读 3+1”当作可写证明。
- `3+1 卡读取记录` 仅为辅助读取痕迹，不再等价于首稿准入完成。
- 未跑 `scene_lint.py`，不得写后验收为通过。
- 命中模板/情节库/事件母板时，未跑 `template_exhaustion_lint.py`，不得写“模板已吃满”。
- 命中 `翻册 / 查账 / 认物 / 抢执行次序 / 公开拍板 / 规则争夺` 题面时，未跑 `scene_narrowness_lint.py`，不得写“中段过线”。
- 未跑 `script_version_check.py`，不得相信项目内脚本结果，也不得判写后验收通过。
- 未跑 `validate_tracking_state.py`，不得判追踪同步完成。
- 生成章组总表、固定验收单、总施工单后，未跑 `verdict_conflict_lint.py`，不得判“可并入”。
- 未补 `追踪/情报台账.md`，情报流不得判完整。
- 任何 `F1-F5` 命中项未回修前，不得进入下一章。
- 任何写后验收不为 `F0`，不得并入主正文。
- 正文落盘后，必须按 `draft_purity_guard.py -> scene_lint.py -> template_exhaustion_lint.py（如适用） -> scene_narrowness_lint.py（如适用） -> 写后验收 -> script_version_check.py -> validate_tracking_state.py -> chapter_hook_repeat_lint.py（连续章时） -> detect_key_character_promotion.py（如适用） -> character_agency_lint.py（如适用） -> 追踪同步 -> verdict_conflict_lint.py -> 下一章前复读追踪` 的顺序收尾；缺任一步，当前章一律视为未完成。
- 只要 `正文/` 最大章节号已经前推，而 `追踪/上下文.md`、`追踪/时间线.md`、`追踪/角色状态.md`、`追踪/伏笔.md`、情报流所需的 `追踪/情报台账.md` 仍停在旧章或缺失，直接按 `F5` 截停，不得继续写下一章，不得自报“已完成本章”。
- 每章正文完成后，下一次任何续写、审查、回炉开始前，都必须先复读并核对：`追踪/上下文.md`、`追踪/时间线.md`、`追踪/角色状态.md`、`追踪/伏笔.md`；涉及情报流时，再加读 `追踪/情报台账.md`。缺任一主表，先补主表，不得直接动正文。
- 章尾必须停在现场，不得停在总结句、预告句、解释句。
- 所有书内资产（`设定/`、`大纲/`、`正文/`、`追踪/`）必须落在对应小说目录下；禁止写到项目根目录。
- `追踪/时间线.md`、`追踪/角色状态.md`、`追踪/情报台账.md`、`追踪/伏笔.md` 只能记录正文已落地事实；未来章节预案、远期细纲事件、未写出的角色变化，必须写在 `大纲/`、`细纲/` 或 `追踪/总施工单_*.md`，禁止混入追踪主表。
- 若任务声明为“纯 skill 冷启动验证 / 回归验证 / 基准测试”，默认进入 `H1-A`：允许保留失败正文作为原样样本，但必须完整落盘失败码、触发规则、样本路径；不得借机改正文冒充首稿成功。
- 若项目是双层目录结构，脚本执行默认以“书目录”为内容根、以“项目根 scripts/”为脚本根；不得因路径分离跳过脚本。

## 每章收尾清单
1. 正文落盘后先跑 `draft_purity_guard.py` 和 `scene_lint.py`，未看结果不得离开正文层。
2. 若本章显式调用模板 / 情节库 / 事件母板，立即补跑 `template_exhaustion_lint.py`；若本章属于 `翻册 / 查账 / 认物 / 抢执行次序 / 公开拍板 / 规则争夺` 题面，再补跑 `scene_narrowness_lint.py`。
3. 脚本过线后立即补齐 `写后验收`，不得用“脚本已过”代替。
4. 写后验收完成后，依次跑 `script_version_check.py`、`validate_tracking_state.py`、连续章钩子与人物专项脚本；任一 `error` 都不得继续。
5. 所有脚本过线后立即同步 `追踪/上下文.md`、`追踪/时间线.md`、`追踪/角色状态.md`、`追踪/伏笔.md`；情报流补 `追踪/情报台账.md`。
6. 同步完成后，若存在章组总表 / 固定验收单 / 总施工单，再跑 `verdict_conflict_lint.py`，确认不存在“可并入 / 不可并入”冲突。
7. 最后反读一遍上述追踪文件，确认章节号、角色状态、事件落点、待回收伏笔都已与正文一致。
8. 上述任一步没做完，本章都不得标记完成，不得进入下一章。

## 回修模式判定
- 先判问题类型，再决定回修方式；禁止默认一路小修到底。
- 仅当问题满足“单点、单层、单因”时，才允许使用“补丁式抠句子”：
  - 只落在单个句子或单个短段
  - 失败码主要是 `F1`
  - 不牵动收益落点、段落骨架、人物站位、追踪口径
- 只要命中以下任一项，强制切换到“一次性收口重写”：
  - 同一章连续 2 轮复审后仍保留相同主失败码
  - 同一问题同时跨“句子层 + 章节层”两层
  - 问题同时涉及“确认链/段落骨架/收益落点/人物压场”中的 2 项及以上
  - 已经出现“局部越改越碎、复审口径反复收窄但不过线”
- 进入“一次性收口重写”后：
  - 先重写问题段骨架，再统一回填验收与工单
  - 禁止继续按句子补丁式来回抠
  - 复审若仍不过线，优先继续整段重写，不回退到零碎小修

## 冲突处理
- 本铁律优先于普通写作建议。
- 与正文、细纲、验收冲突时，以本铁律为准，先改文件，再继续写。
