# 写作执行铁律

## 目的
- 统一本项目的写作硬闸，避免不同会话、不同章节、不同批次各说各话。
- 若项目内存在明确书名目录，`写作执行铁律.md` 必须写入对应小说目录，不得落到项目根目录。

## 读前顺序
1. 先读 `写作执行铁律.md`
2. 再读当前书目的 `追踪/上下文.md`
3. 再读当前章的 `写前闸门`、`细纲`、`写后验收`

## 硬闸
- 未写写前闸门，不得写正文。
- 未跑 `scene_lint.py`，不得写后验收为通过。
- 未补 `追踪/情报台账.md`，情报流不得判完整。
- 任何 `F1-F5` 命中项未回修前，不得进入下一章。
- 任何写后验收不为 `F0`，不得并入主正文。
- 章尾必须停在现场，不得停在总结句、预告句、解释句。
- 所有书内资产（`设定/`、`大纲/`、`正文/`、`追踪/`）必须落在对应小说目录下；禁止写到项目根目录。
- `追踪/时间线.md`、`追踪/角色状态.md`、`追踪/情报台账.md`、`追踪/伏笔.md` 只能记录正文已落地事实；未来章节预案、远期细纲事件、未写出的角色变化，必须写在 `大纲/`、`细纲/` 或 `追踪/总施工单_*.md`，禁止混入追踪主表。

## 回修模式判定
- 先判问题类型，再决定回修方式；禁止默认一路小修到底。
- 仅当问题满足“单点、单层、单因”时，才允许使用“补丁式抠句子”：
  - 只落在单个句子或单个短段
  - 失败码主要是 `F1`
  - 不牵动收益落点、段落骨架、人物站位、追踪口径
- 只要命中以下任一项，强制切换到“一次性收口重写”：
  - 同一章连续 2 轮复审后仍保留相同主失败码
  - 同一问题同时跨“句子层 + 章节层”两层
  - 问题同时涉及“确认链/段落骨架/收益落点/人物压场”中的 2 项及以上
  - 已经出现“局部越改越碎、复审口径反复收窄但不过线”
- 进入“一次性收口重写”后：
  - 先重写问题段骨架，再统一回填验收与工单
  - 禁止继续按句子补丁式来回抠
  - 复审若仍不过线，优先继续整段重写，不回退到零碎小修

## 冲突处理
- 本铁律优先于普通写作建议。
- 与正文、细纲、验收冲突时，以本铁律为准，先改文件，再继续写。
